/*
 * Week 3 - Rules and ownership
 * Invariant: the balance is never negative.
 */
public class Account {

    // TODO private static int lastNumberIssued = 1000;

    // TODO private final String accountNumber, generated in the constructor
    // TODO private String ownerName, private double balance

    // TODO constructor takes ownerName and openingBalance only.
    //      Rejects a blank owner and a negative balance.
    //      Put the bad value in every message.

    // TODO private static requireText(String value, String label)
    //      One rule, one place. Static because it touches no fields.

    // TODO setOwnerName: allowed, because names change. Validates.
    //      There is still no setBalance and there never will be.

    // TODO deposit(double): void. Throws on a non-positive amount.

    // TODO withdraw(double): throws on a non-positive amount,
    //      returns false when there is not enough money.

    // TODO getAccountsOpened(): static
}