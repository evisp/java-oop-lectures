/*
 * Week 4 - Objects that own other objects
 */
public class Customer {

    // TODO private static int lastIdIssued = 1000;
    // TODO private final String customerId, private String name
    // TODO private final List<Account> accounts = new ArrayList<>();
    //      private so nobody outside touches it, final so it is never
    //      swapped, created here so no customer exists without one.

    // TODO openAccount(double): makes it, adds it, returns it

    // TODO findAccount(String): null when there is no match

    // TODO closeAccount(String): refuses while the balance is above zero

    // TODO withdrawFrom(String, double): find the account and ASK it.
    //      No arithmetic here. No balance rules here.

    // TODO getTotalBalance(): calculated, never stored

    // TODO getAccounts(): List.copyOf(accounts), never the list itself
}